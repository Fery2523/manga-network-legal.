from flask import Flask, render_template

app = Flask(__name__)

@app.route('/')
def home():
    return "Bienvenido a Manga Network - Próximamente la mejor lectura legal"

if __name__ == '__main__':
    app.run(debug=True)
